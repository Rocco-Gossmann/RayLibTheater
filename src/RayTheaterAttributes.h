// Header to keep the IDEs / LSPs happy.
#ifndef STAGE_ATTRIBUTE 
#define STAGE_ATTRIBUTE(name) /* NOP */
#endif

/* Define your Attributes here:
 * They can then be accessed via the Stage::Attributes enum 
 * (included via ./stage/components/attributes.h)

// !!! NAMING must follow the Variale name rules !!!
// example:

STAGE_ATTRIBUTE(EXAMPLE_ATTRIBUTE)

//  Put your Attributes after this line. From then they can be accessed via.
//============================================================================*/


